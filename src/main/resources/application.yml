spring:
  application:
    name: clothing-inventory

aws:
  dynamodb:
    region: ap-northeast-2
    endpoint: # 로컬 개발 환경에서 DynamoDB Local 사용 시: http://localhost:8000
    # 프로덕션 환경에서는 endpoint를 설정하지 않으면 AWS 서비스에 자동 연결됨
